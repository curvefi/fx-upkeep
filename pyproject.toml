[project]
name = "fx-upkeep"
version = "0.1.0"
description = "YieldBasis Twocrypto upkeep keeper"
requires-python = ">=3.12"
dependencies = [
    "eth-account>=0.13,<1",
    "httpx>=0.28,<1",
    "python-dotenv>=1,<2",
    "pyyaml>=6,<7",
    "web3>=7.12,<8",
]

[dependency-groups]
dev = [
    "ruff>=0.12,<1",
]

[tool.ruff]
line-length = 100

